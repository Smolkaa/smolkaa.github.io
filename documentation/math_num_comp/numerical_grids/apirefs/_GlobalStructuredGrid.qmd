::: {.callout-tip collapse="true"}
# ExESS API -- Global Structured Grids

Global, structured grids are implemented in `ExESS` as [`GlobalStructuredGrid`](/api/grids/global_structured_2d_grids.qmd).

For utility functions, see: `areas`, `coords`, `coord2idx`, `surfacecoords`, and `volumes`.

:::